name: Claude Code Local Action
on: [push, workflow_dispatch]

jobs:
  build-website:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Ollama
        uses: ai-action/setup-ollama@v2
        with:
          model: qwen2.5-coder:7b # Modelo optimizado para el límite de 7GB RAM de GitHub

      - name: Install Claude Code
        run: |
          npm install -g @anthropic-ai/claude-code

      - name: Run Claude (Bypass Mode)
        env:
          ANTHROPIC_BASE_URL: http://localhost:11434
          ANTHROPIC_AUTH_TOKEN: ollama
        run: |
          # Usamos un nombre que Claude reconoce, pero Ollama lo interceptará 
          # y servirá el modelo que cargamos en el paso anterior
          claude -p "$(cat requirements.txt)" --model claude-3-5-sonnet-20241022 --dangerously-skip-permissions



      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .
          
          if git diff --quiet && git diff --staged --quiet; then
            echo "No hay cambios para subir."
          else
            git commit -m "feat: website updated by Claude Code (Local Qwen)"
            git push
          fi
